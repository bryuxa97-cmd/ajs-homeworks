# Задача 1. for ... in

Функция `orderByProps(obj, order)` возвращает массив свойств объекта
в формате `{ key, value }`: сначала ключи из `order`, затем остальные по алфавиту.

```bash
npm install
npm run lint
npm run coverage
```
