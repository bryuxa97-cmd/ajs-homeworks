# Задача 2. Destructuring

Функция `extractSpecial({ special })` извлекает `id`, `name`, `description`, `icon`.
Если `description` нет — подставляется `'Описание недоступно'`.

```bash
npm install
npm run lint
npm run coverage
```
